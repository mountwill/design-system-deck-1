import theme from "mdx-deck/themes";

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: "Change Calibre",

  // custom colors
  colors: {
    text: "#fff",
    background: "#ec2c22",
    link: "#fff"
  }
};
